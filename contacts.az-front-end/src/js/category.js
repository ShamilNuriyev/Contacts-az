function searchByCategory1() {
    const btn = "Education"
    const filteredCharacters = hpCharacters.filter((character) => {
        return (
            character.category.includes(btn)
        );
    });
    displayCharacters(filteredCharacters);
}

function searchByCategory2() {
    const btn = 'Cleaning'
    const filteredCharacters = hpCharacters.filter((character) => {
        return (
            character.category.includes(btn)
        );
    });
    displayCharacters(filteredCharacters);
}

function searchByCategory3() {
    const btn = "Delivery"
    const filteredCharacters = hpCharacters.filter((character) => {
        return (
            character.category.includes(btn)
        );
    });
    displayCharacters(filteredCharacters);
}

function searchByCategory4() {
    const btn = 'Bakery'
    const filteredCharacters = hpCharacters.filter((character) => {
        return (
            character.category.includes(btn)
        );
    });
    displayCharacters(filteredCharacters);
}
function searchByCategory5() {
    const btn = "Driving"
    const filteredCharacters = hpCharacters.filter((character) => {
        return (
            character.category.includes(btn)
        );
    });
    displayCharacters(filteredCharacters);
}

function searchByCategory6() {
    const btn = 'HomeJobs'
    const filteredCharacters = hpCharacters.filter((character) => {
        return (
            character.category.includes(btn)
        );
    });
    displayCharacters(filteredCharacters);
}

function searchByCategory7() {
    const btn = "Business"
    const filteredCharacters = hpCharacters.filter((character) => {
        return (
            character.category.includes(btn)
        );
    });
    displayCharacters(filteredCharacters);
}

function searchByCategory8() {
    const btn = 'Marketing'
    const filteredCharacters = hpCharacters.filter((character) => {
            return (
                character.category.includes(btn)
            );
    });
    displayCharacters(filteredCharacters);
}

function searchByCategory9() {
    const btn = "Sport"
    const filteredCharacters = hpCharacters.filter((character) => {
        return (
            character.category.includes(btn)
        );
    });
    displayCharacters(filteredCharacters);
}

function searchByCategory10() {
    const btn = 'IT'
    const filteredCharacters = hpCharacters.filter((character) => {
        return (
            character.category.includes(btn)
        );
    });
    displayCharacters(filteredCharacters);
}










